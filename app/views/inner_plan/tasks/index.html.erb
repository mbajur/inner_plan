<div class="row justify-content-center mt-4">
  <div class="col-6">
    <% @lists.each do |list| %>
      <%= render partial: 'inner_plan/lists/list_root', locals: { list: list } %>
    <% end %>
  </div>
</div>
