<turbo-stream action="update" target="<%= dom_id(@list, :add_task) %>">
  <template>
    <%= render partial: 'inner_plan/tasks/inline_form', formats: [:html], locals: { list: @list, task: @task, form_visible: true } %>
  </template>
</turbo-stream>
