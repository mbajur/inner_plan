<turbo-stream action="update" target="<%= dom_id(@list, :add_task) %>">
  <template>
    <%= render InnerPlan::Tasks::InlineFormView.new(list: @list, task: @task, form_visible: true) %>
  </template>
</turbo-stream>
